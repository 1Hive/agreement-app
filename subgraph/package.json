{
  "name": "@aragon/subgraph-agreement",
  "version": "0.1.0",
  "scripts": {
    "codegen": "rm -rf generated; graph codegen",
    "build": "rm -rf build; graph build",
    "manifest-goerli": "scripts/build-manifest.sh goerli",
    "manifest-goerli-staging": "env STAGING=true scripts/build-manifest.sh goerli",
    "manifest-gnosis": "scripts/build-manifest.sh gnosis",
    "manifest-polygon": "scripts/build-manifest.sh polygon",
    "deploy-goerli": "scripts/deploy.sh goerli",
    "deploy-goerli-staging": "env STAGING=true scripts/deploy.sh aragon agreement goerli",
    "deploy-gnosis": "scripts/deploy.sh gnosis",
    "deploy-polygon": "scripts/deploy.sh polygon"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.39.0",
    "@graphprotocol/graph-ts": "^0.29.3",
    "mustache": "^4.0.1"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
